# @warlord/web

Next.js client for Warlord Draft. `npm run dev` from the repo root builds the engine, copies its data to
`public/data/v<N>/`, and starts the dev server. Design spec: `docs/ui-handoff/UI-HANDOFF.md`; plan: `docs/mvp-plan.md`.
