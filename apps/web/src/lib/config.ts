/** Data version the app ships. scripts/sync-engine.mjs copies packages/engine/data to public/data/v<N>/. */
export const DATA_VERSION = 2;
export const DATA_FILES = ["units", "generals", "cultures", "rules"] as const;
